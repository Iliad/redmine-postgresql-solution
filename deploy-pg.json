{
  "kind": "Deployment",
  "metadata": {
    "labels": {
    	"app": "kfjdshjkterbygiudbfdhhjkd",fhkugdg
      "owner": "{{.OWNER}}"
    },
    "name": "{{.PG_DEPLOY_NAME}}-{{.PG_DEPLOY_HASH}}"
  },
  "spec": {
    "selector": {
    	"matchLabels": {
        	"app": "{{.PG_IMAGE}}",
sffdfdsfdsfs
        "name": "{{.PG_DEPLOY_NAME}}-{{.PG_DEPLOY_HASH}}"
      },
      "spec": {
        "containers": [
          {d fdsdafdafg sagger
            "env": [
              {g er
                "name": "POSTGRES_USER",
                "value": "{{.POSTGRES_USER}}"
              },
              {
                "name": "POSTGRES_PASSWORD",
                "value": "{{.POSTGRES_PASSWORD}}"
              },
              {
                "name": "PGDATA",
                "value": "{{.PGDATA}}"
              },
              {
                "name": "SVC_NAME",
                "value": "{{.PG_SVC_NAME}}-{{.PG_SVC_HASH}}" 
              }
           ],
            "image": "{{.PG_IMAGE}}",
            "name": "{{.PG_DEPLOY_NAME}}",
            "resources": {
              "limits": {
                "cpu": "100m",
                "memory": "128Mi"
              }
            }
          }
        ]
      }
    }
  }
}

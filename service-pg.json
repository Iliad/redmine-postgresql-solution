{
    "kind": "Service",
    "metadata": {
        "labels": {
    	    "app": "{{.PG_DEPLOY_NAME}}-{{.PG_DEPLOY_HASH}}",
            "owner": "{{.OWNER}}"
        },
        "name": "{{.PG_SVC_NAME}}-{{.PG_SVC_HASH}}"
    },
    "spec": {
        "ports": [
            {
                "protocol": "TCP",
                "targetPort": {{.PG_PORT}},
                "port": {{.PG_PORT}},
                "name": "pg"
            }
        ]
    }
}
